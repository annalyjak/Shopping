<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.alyjak.shopping">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:label="Add product"
                android:name=".ui.addproduct.NewProductActivity">
        </activity>
        <activity
                android:label="Details"
                android:name=".ui.details.ProductsActivity"
                android:parentActivityName=".ui.MainActivity">
        </activity>
        <activity
                android:label="Create new shopping list"
                android:name=".ui.createnewlist.CreateShoppingListActivity">
        </activity>
        <activity
                android:name=".ui.MainActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>